{
  "name": "topup-deposit-app",
  "version": "1.0.0",
  "description": "Mobile-first deposit & UPI recharge web app (Express backend + vanilla JS frontend)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "helmet": "^7.1.0"
  }
}
